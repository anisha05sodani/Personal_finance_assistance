# Frontend environment configuration.
# Copy this file to ".env" (or ".env.local") and adjust if your backend runs
# on a non-default port. Vite only exposes variables prefixed with VITE_.

# Base URL of the backend API. Defaults to http://localhost:8000 when unset.
VITE_API_URL=http://localhost:8000
