# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# --- OCR engine ---
# Pinned to the 2.7.x API (ocr.ocr(...) returns [[box, (text, conf)], ...]).
paddleocr==2.7.3
paddlepaddle==2.6.1

# --- Image / PDF processing ---
opencv-python-headless>=4.9
numpy>=1.24,<2.0
PyMuPDF>=1.24
Pillow>=10.0

# --- Testing ---
pytest>=8.0
httpx>=0.27
