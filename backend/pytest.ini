[pytest]
# Run pytest from the backend/ directory.
# `pythonpath = .` puts backend/ on sys.path so `import app...` resolves.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
